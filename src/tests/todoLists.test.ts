import { v1 } from "uuid";
import { TodoListType } from "../constants/types";
import { todoListAPI } from "../api/todoistAPI";
import { addTodoList, addTodoTC } from "../store/slices/todoLists.slice";

test("Correct Todo should be added", () => {
  
});
